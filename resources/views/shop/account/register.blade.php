@extends('shop.layout', ['channel' => app(\App\Shop\CatalogService::class)->channel()])
@section('title', 'Create customer account')
@section('content')
<div class="account-card"><p class="eyebrow">CUSTOMER ACCOUNT</p><h1>Create your account</h1>@if($errors->any())<div class="shop-notice error" role="alert">Please correct the highlighted fields.</div>@endif
<form method="POST" action="{{ route('shop.account.register.store') }}">@csrf<label>Full name<input name="name" value="{{ old('name') }}" required autocomplete="name">@error('name')<small>{{ $message }}</small>@enderror</label><label>Email<input type="email" name="email" value="{{ old('email') }}" required autocomplete="email">@error('email')<small>{{ $message }}</small>@enderror</label><label>Mobile<input name="mobile" value="{{ old('mobile') }}" autocomplete="tel"></label><label>Password<input type="password" name="password" required autocomplete="new-password">@error('password')<small>{{ $message }}</small>@enderror</label><label>Confirm password<input type="password" name="password_confirmation" required autocomplete="new-password"></label><label class="terms"><input type="checkbox" name="terms" value="1" required> I accept the shop terms and privacy notice.</label><button class="shop-button" type="submit">Create account</button></form><p>Already registered? <a href="{{ route('shop.account.login') }}">Log in</a></p></div>
@endsection

@extends('shop.layout')
@section('title', 'Checkout · '.$channel->name)
@section('content')
<p class="eyebrow">SECURE CHECKOUT</p><h1>Your details</h1>
@if($errors->any())<div class="shop-notice error" role="alert"><strong>Please check the highlighted information.</strong></div>@endif
<div class="cart-layout"><form class="checkout-form" method="POST" action="{{ route('shop.checkout.store') }}">@csrf<div class="form-grid"><label>Full name<input name="name" value="{{ old('name') }}" required autocomplete="name"></label><label>Email<input type="email" name="email" value="{{ old('email') }}" required autocomplete="email"></label><label>Mobile number<input name="mobile" value="{{ old('mobile') }}" required autocomplete="tel"></label><label>Address line 1<input name="address_line_1" value="{{ old('address_line_1') }}" required autocomplete="address-line1"></label><label>Address line 2<input name="address_line_2" value="{{ old('address_line_2') }}" autocomplete="address-line2"></label><label>City<input name="city" value="{{ old('city') }}" required autocomplete="address-level2"></label><label>Province<input name="province" value="{{ old('province', 'Western Cape') }}" required autocomplete="address-level1"></label><label>Postal code<input name="postal_code" value="{{ old('postal_code') }}" required autocomplete="postal-code"></label></div><label class="terms"><input type="checkbox" name="terms" value="1" required> I confirm these details and accept the shop terms.</label><button class="shop-button" type="submit">Reserve order and continue</button></form><aside class="order-summary"><h2>Collection order</h2>@foreach($items as $item)<div><span>{{ $item->quantity }} × {{ $item->shopVariation->shopProduct->product->name }}</span><strong>R {{ number_format($item->shopVariation->variation->sell_price_inc_tax * $item->quantity, 2) }}</strong></div>@endforeach<div class="summary-total"><span>Total</span><strong>R {{ number_format($subtotalCents / 100, 2) }}</strong></div><p>No payment is taken until PayFast confirms it.</p></aside></div>
@endsection
